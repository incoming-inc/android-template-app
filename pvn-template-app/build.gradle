buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.1'
    }
}

allprojects {
    repositories {
        jcenter()
        maven {
            url "http://107.178.209.67:8081/nexus/content/repositories/releases/"
        }

        flatDir {
            dirs '/Users/dougwright/workspaces/Incoming/dev_pvn_v1/sdk/Incoming-SDK/build/outputs/aar/'
        }

    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.14.1'
}
